<Window x:Class="Z25023_Mostostal.Windows.CuttingDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dane Cyklu Cięcia z PLC" Height="750" Width="600"
        WindowStartupLocation="CenterOwner" Background="#F0F0F0" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="ZGENERALIZOWANY PROFIL CIĘCIA W PLC (Task 60)" Grid.Row="0" 
               Style="{StaticResource HeadersLabel}" HorizontalAlignment="Stretch" Height="40" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Aktywne Stemple w wybranym kroku" BorderBrush="DodgerBlue" Margin="0,0,0,10" Foreground="DodgerBlue" FontWeight="Bold">
            <StackPanel Margin="5" HorizontalAlignment="Center">

                <ItemsControl Name="EvenPunchesVisualizer">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="DarkGray" BorderThickness="1" Margin="2" Width="28" Height="32" CornerRadius="3"
                                    Background="{Binding BackgroundBrush}">
                                <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           FontWeight="Bold" Foreground="{Binding TextBrush}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Name="OddPunchesVisualizer" Margin="16,0,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="DarkGray" BorderThickness="1" Margin="2" Width="28" Height="32" CornerRadius="3"
                                    Background="{Binding BackgroundBrush}">
                                <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           FontWeight="Bold" Foreground="{Binding TextBrush}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </GroupBox>

        <DataGrid Name="CuttingGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                  HeadersVisibility="Column" Background="White" BorderBrush="DodgerBlue" BorderThickness="1"
                  RowHeight="30" FontSize="14" AlternatingRowBackground="#F8F8F8"
                  SelectionChanged="CuttingGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Krok" Binding="{Binding Lp}" Width="55">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Przesuw w Pętli (mm)" Binding="{Binding Delta, StringFormat=F2}" Width="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="DarkGreen"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="10,0,0,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Maska Stempli (Punch)" Binding="{Binding Punch}" Width="160">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Cięcie" Binding="{Binding Cut}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="1">
                                    <Setter Property="Foreground" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Zamknij" Width="100" Height="35" Margin="0,15,0,0"
                Style="{StaticResource StandardButton}" HorizontalAlignment="Right" IsCancel="True" Click="Close_Click"/>
    </Grid>
</Window>